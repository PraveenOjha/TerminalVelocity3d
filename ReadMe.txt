App .zip Contain executable unzip and use.
xna 4.0 redist needed ,Intsall form same folder as zip file 




Read Me for app /prototype use .

This software will be best used with visual studio 2012 .
As this game use content generated from xna .Which is packed in app .zip
All files inside app.zip should be present in release/debug folder 
For example 
if  your configuration is debug and while compilation then 
all files inside app.zip will go inside bin/debug/
similarly for cases with x86 selected 
bin/x86/debug
or bin/x86/release